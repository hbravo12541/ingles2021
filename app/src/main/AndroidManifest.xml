<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.simulador.ingles">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".modulo2"></activity>
        <activity android:name=".modulo1">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".order_words" />
        <activity android:name=".module1_hearAndWrite" />
        <activity android:name=".module1_identify" />
        <activity android:name=".module1_adjetives" />
        <activity android:name=".login_first" />
        <activity android:name=".MainActivity"></activity>
    </application>

</manifest>